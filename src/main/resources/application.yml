spring:
  main:
    class: com.studies.todo_app.TodoAppApplication
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:todo-app
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
